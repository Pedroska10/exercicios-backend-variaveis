console.log('Soma dos ângulos internos de um polígono');
let n = 6
const s = (n - 2) * 180;
if (n % 2 === 0) {
    const a = s / n
    console.log(`A soma dos angulos internos é de ${s} e o valor de cada angulo em graus é de ${a}`)
} else {
    console.log(`A soma dos angulos internos é de ${s} e o valor de cada angulo em graus é de ${a}`)
}